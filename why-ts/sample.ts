const add = (a: number, b: number): number => {
  return a + b;
};

const calResult = add(10, 20);

//여기서 calResult. 찍으면 바로 number와 관련된 api들을 볼 수 있다.
//이게 바로 vsCode의 intellisence? 인텔리센스라고 하는 기능이다. 즉 코드 자동 완성이다.
